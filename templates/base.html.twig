<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Мой сайт{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">

    {% block stylesheets %}{% endblock %}
</head>
<body class="d-flex flex-column min-vh-100">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="/home">Reviews</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item {% if app.request.pathinfo == '/home' %}active{% endif %}">
                    <a class="nav-link" href="/home">Главная</a>
                </li>
                <li class="nav-item {% if app.request.pathinfo == '/about' %}active{% endif %}">
                    <a class="nav-link" href="/about">О нас</a>
                </li>
                <li class="nav-item {% if app.request.pathinfo == '/contact' %}active{% endif %}">
                    <a class="nav-link" href="/contact">Контакты</a>
                </li>
                <li class="nav-item {% if app.request.pathinfo == '/register' %}active{% endif %}">
                    <a class="nav-link" href="/register">Регистрация</a>
                </li>

                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.name }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                            <li><a class="dropdown-item" href="/home">Профиль</a></li>
                            <li><a class="dropdown-item" href="{{ path('logout') }}">Выйти</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item {% if app.request.pathinfo == '/login' %}active{% endif %}">
                        <a class="nav-link" href="{{ path('login') }}">Вход</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>


<main class="flex-fill">
    <div class="container py-4">
        {% block body %}{% endblock %}
    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
